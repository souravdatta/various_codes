# APL
FindUniqueNumbers ← {≢∪((⊢∊∘⎕D)⊆⊢)⍵}
ForwardReplace ← {⍺ ⊣⍣(⍵=0)⊢ ⍵}\

⎕IO ← 0
Comp ← {</2↑⍵}
CountIncreases ← {+/(Comp∘⊢↓∘⍵)¨(⍳≢⍵)}


def find_unique_numbers(s)
  Set.new(s.scan(/\d+/)).count
end
